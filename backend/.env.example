# ── AI ──────────────────────────────────────────────────
ANTHROPIC_API_KEY=

# Set to "true" to use the Claude Agent SDK runner instead of the raw API loop.
# Default: false (original hand-rolled agent loop). Instant rollback: set back to false.
AFINDR_USE_SDK_RUNNER=false

# ── Market Data ─────────────────────────────────────────
POLYGON_API_KEY=
FINNHUB_API_KEY=
FRED_API_KEY=
BLS_API_KEY=

# ── Auth (Convex JWT validation) ────────────────────────
CONVEX_DEPLOYMENT_URL=
CORS_ORIGINS=http://localhost:3000
# Set to "true" in production to reject unauthenticated requests.
# Default: false (dev mode — tokens validated if present, but unauthenticated requests allowed).
AFINDR_REQUIRE_AUTH=false

# ── Admin ───────────────────────────────────────────────
ADMIN_API_KEY=

# ── Sandbox ─────────────────────────────────────────────
SANDBOX_MODE=thread
